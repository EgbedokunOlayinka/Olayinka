let name = 'Egbedokun Olayinka';
let courses = ['HTML', 'CSS', 'Javascript', 'Node.js'];
console.log(name);
console.log(courses);

for (num = 1; num <= 200; num++) {
    if (num % 2 == 0) {
        console.log(num);
    }
}